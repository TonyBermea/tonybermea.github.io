# Tony Bermea – Experience Engineer

This repository hosts the static export served via GitHub Pages at [tonybermea.dev](https://tonybermea.dev). The artifacts in `_next/static` come from the upstream Next.js project and should be treated as generated files.

## Contributor Guide

Before proposing changes, read [`AGENTS.md`](AGENTS.md) for expectations around structure, build steps, formatting, and review checklists.

## Local Preview

To verify modifications, run:

```bash
python3 -m http.server 3000
```

Then visit `http://localhost:3000` to inspect the static export locally.
